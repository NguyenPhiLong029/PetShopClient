export enum OrderStatus {
  InCart = 'in-cart',
  InProgress = 'in-progress',
  InDelivery = 'in-delivery',
  Done = 'done',
  Cancel = 'cancel'
}

export enum UserRole {
  Admin = 'admin',
  Staff = 'staff',
  Customer = 'customer'
}
